// тут будут запросы, а пока только тип

export interface bankProps {
  name: string;
  address: string;
  timeWalk: string;
  timeCar: string;
  status: boolean;
  rating: number;
  distance: number;
  openTime: number;
  closeTime: number;
}
